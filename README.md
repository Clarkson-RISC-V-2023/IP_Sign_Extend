# IP_Template <IP Name>
- # Description of IP
- # Build Instructions
- # File Hierarchuy

# Submodules:
## Steps to add a new one:
`git submodule add -b main -f --name <repo_name> <ssh_to_repo> <local_path>`
